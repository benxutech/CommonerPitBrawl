// 2 types of button, Big (menu), Small (which just manage to fit the text)

var b = instance_create(argument1,argument2,objButton);
b.owner = argument0;
b.label = argument3;
b.trigger = argument4;

return b;
